# 3-5-exercises-Input01-Input03-

Changes to files
Input01
* Declared a String with JOptionPane.
* Parsed String as an int and incremented it, then made it print the value.
* Did the same as above, but within a single line with one semicolon ; (the print exists to show the value it shouldn't count.)
Input03
* Created the scanner object (myScan).
* Before input, displays a print message informing user to input three values.
* Declared three ints, each containing nextInt() methods.
* Another int that adds the former three into a sum.
* Made it print the sum.
* Of course, made it close scanner after executing commands
